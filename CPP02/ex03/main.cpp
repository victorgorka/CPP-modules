#include <iostream>
#include "Point.hpp"

// bool bsp( Point const a, Point const b, Point const c, Point const point)
// {
// 	float w1, w2, e, d;

// }

int main( void ) {
	Fixed a(2), b(4.5f);

	std::cout << "a1 + a2" << a + b;
	return 0;
}
